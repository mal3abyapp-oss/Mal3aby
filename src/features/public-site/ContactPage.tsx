import { PlaceholderPage } from '@/components/ui/placeholder-page'
// Form → contact_requests (anon insert-only). Built in Phase 3d.
export function ContactPage() {
  return <PlaceholderPage title="تواصل معنا" phase="Phase 3d" />
}
