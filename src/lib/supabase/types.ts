// Placeholder — regenerated from the real schema once migrations exist,
// via `supabase gen types typescript`. See docs/PROJECT_RULES.md rule 59:
// never let the frontend depend on stale generated types.
export type Database = Record<string, never>
