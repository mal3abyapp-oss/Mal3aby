# Mal3aby — Full End-to-End Product Acceptance

Real business journey / cross-module production hardening.
Started: 2026-08-31. Mode: long-run autonomous foreground execution.

Baseline commit: `3c94b8a` (Customer Email OTP Authentication = CLOSED
PRODUCTION BASELINE, and all prior domain baselines listed in Section 0
of the directive). This mission does NOT reopen those baselines unless a
concrete cross-module journey exposes a reproducible regression.

## Status legend

PENDING · IN PROGRESS · PASS · FIXED + PASS · ACCEPTED LIMITATION ·
ENVIRONMENT-BLOCKED · TRUE BLOCKER

## Severity legend

P0 (blocks the business) · P1 (serious, wrong money/data) · CORE P2
(journey-relevant defect) · P3 (optional/cosmetic, not chased
indefinitely)

Closure threshold: P0 = 0, P1 = 0, CORE P2 = 0.

## QA fixtures (disposable, tracked for cleanup)

| Role | Identifier | Notes |
|---|---|---|
| Tenant A (primary) | `f8376f07-2b53-4146-a7fd-411c2672115a` ("Mal3aby E2E QA Club") | created in Section 4 |
| Tenant B (isolation-attack only) | TBD | created in Section 23 |
| Owner | `7ca462fd-5ad5-4131-b6a9-fa8e7e68deac` (mal3aby.e2e.owner@example-qa-fixture.test) | auth.users row, no password (SQL-impersonated only, see Notes) |
| Staff — front desk | `ae009fa7-f21e-4835-b8b6-f20338d2c899` (receptionist, mal3aby.e2e.frontdesk@example-qa-fixture.test) | active (D-E2E-001 fix verified) |
| Staff — restricted | `42749343-1ec3-431d-bfde-bacaa46c788e` (coach, mal3aby.e2e.coach@example-qa-fixture.test) | active (D-E2E-001 fix verified) |
| Customer 1 (booking/membership) | `e39e1200-6c90-4f40-a793-705e107ac238` ("أحمد محمود E2E", +201099988877, moustafa.elsafy2+e2ecustomer@gmail.com) | |
| Customer/family 2 (academy) | Guardian `83e90285` (سارة عبد الله E2E) / Player `20baf3d9` (يوسف E2E) | |

## Journey tracker

| # | Section | Status | Severity if defect | Evidence |
|---|---|---|---|---|
| 4 | Tenant creation | PASS | | SERVER VERIFIED: `complete_new_club_onboarding` invoked via real JWT-claim impersonation of a real QA `auth.users` row. Club active, branch (MAIN), owner `club_membership` (club_owner, cash custody), default modules (fields/academy/club_membership on, shop off), 7-day trial subscription, `club.onboarded` audit log all correct. Retry-with-same-name correctly `flagged_duplicate=true` (not a silent duplicate — real business rule) and correctly did NOT grant a second trial (`automatic_trial_entitlements` unique constraint held). Retry artifact cleaned up. |
| 5 | Tenant owner first entry | PASS | | SERVER VERIFIED (RLS-scoped as real owner via JWT impersonation, exact client queries): `club_memberships` returns exactly 1 row (own club, `club_owner`, active). `caller_permission_keys()` returns full owner permission set with zero `platform.*` keys (no platform-owner privilege leak). RLS-scoped `clubs`/`club_memberships` counts = 1 each (no other-tenant data visible). `platform_subscriptions` correctly shows `trial`/`trial` matching the onboarding-time 7-day window. |
| 6 | Minimum setup | PASS | | SERVER VERIFIED via real RPCs (owner-impersonated): field `c4de807b` created via `manage_field`, 12 pricing rules (weekday/weekend, day/night) via `create_field_pricing_rules`. Membership plan `546183bc` (500 EGP/month) via `create_club_membership_plan`. Academy group `d2a4a5cd` (U10, cap 20, 400 EGP) via direct RLS-governed `groups` insert (the real client path — no dedicated RPC exists for group creation). |
| 7 | Staff creation | FIXED + PASS | P0 (D-E2E-001) | Created via the real `create_club_staff_membership_service` RPC (the actual `club-staff-admin` Edge Function logic replicated exactly: `auth.admin.createUser`-equivalent insert + governed RPC, service_role-only grant confirmed). Found and fixed D-E2E-001 (see defect register) blocking all staff from ever gaining access. Post-fix: receptionist has front-desk permission set (booking/payment/customer/invoice, no admin/financial-config keys); coach has genuinely restricted set (sessions/attendance/QR only, zero financial permissions). Both branch-scoped to the one real branch. |
| 8 | Staff start-of-day | PASS | | SERVER VERIFIED: receptionist opened a real cash shift (`f322595c`, 500 EGP opening float) via `open_cash_shift`, correct club/branch scoping. Correct dashboard access confirmed via the permission-key check in Section 7 (booking/payment/customer keys present). |
| 9 | Customer creation | PASS | | SERVER VERIFIED: customer `e39e1200` ("أحمد محمود E2E") created via `upsert_customer` (`was_existing=false`), email set for portal OTP testing. `get_customer_360_summary` correctly reflects the new customer with correct zero-state financials. |
| 10 | Customer Email OTP | PASS | | LIVE VISUALLY VERIFIED in production (`/portal/login`, real customer email): OTP request succeeded (transitioned to code-entry stage), wrong-OTP correctly rejected with the generic non-raw error, no duplicate `auth.users` row from the failed attempt. SERVER VERIFIED: exactly 1 `auth.identities` row (`email` provider), OTP token stored hashed (not plaintext-readable — correctly enforced), customer NOT auto-linked by OTP alone (`user_id` stayed null until the separate hardened claim RPC ran). Claimed via `claim_customer_self_service` (SQL-impersonated, same RPC the real portal uses) — correctly resolves to the right customer + right club. ENVIRONMENT-BLOCKED: completing the full live round-trip by reading the real OTP from the inbox (no email-reading tool available) — same class of credential/tooling gap as every prior phase's live-login constraint, not a defect. |
| 11 | Booking + lifecycle | PASS | | SERVER VERIFIED: booking `ac154008` created via real `create_booking` RPC (evening pricing correctly applied: 250 EGP, matches the pricing rule seeded in Section 6), auto-created invoice (`a55a47f3`, issued, 250). Double-booking correctly rejected with a clear non-technical error. Full payment correctly auto-confirmed the booking (`pending_payment→confirmed`). `mark_booking_completed` CODE VERIFIED (correctly time-gated — rejects completion before `end_at`, correctly permission/branch-scoped, correctly audited) — two genuine temporal-integrity RPC rejections encountered and confirmed correct (can't complete before end, can't create a booking in the past). Cancellation verified live: `cancel_booking` correctly transitions to `cancelled` with reason + timestamp. |
| 12 | Payment | PASS | | SERVER VERIFIED: `record_payment` (cash) correctly allocates against the invoice, correctly triggers booking auto-confirmation on full payment, correct invoice/payment/booking state agreement. |
| 13 | Partial payment | PASS | | SERVER VERIFIED: 150/250 EGP partial payment correctly left `outstanding=100`, booking correctly stayed `pending_payment` (not auto-confirmed on partial) until the remaining 100 was paid. |
| 14 | Refund/reversal | PASS | | SERVER VERIFIED: `create_refund` (100 EGP partial refund on the 250 EGP fully-paid booking) correctly recorded, invoice/payment-allocation math agrees exactly (total=250, gross_paid=250, refunded=100), booking correctly stays `confirmed` (a partial refund on a delivered booking doesn't un-confirm it). Cross-module agreement confirmed: `get_customer_360_summary` independently shows the identical figures (`total_paid=250, total_invoiced=250, total_refunded=100`). |
| 15 | Club membership + renewal | PASS | | SERVER VERIFIED: `sell_club_membership` created MEM-000001 (1-month plan, correctly computed 2026-08-31→2026-09-29), full payment correctly activated it (`pending_payment→active`, confirms the `_activate_club_membership_if_due_internal` mechanism found earlier IS wired for this table). `renew_club_membership` correctly created a NEW subscription record (MEM-000002, starts the day after the original ends: 2026-09-30, correctly `pending_payment` until paid) rather than mutating history — both records remain queryable. |
| 16 | Academy enrollment + attendance | PASS | | SERVER VERIFIED: player يوسف created via `create_player_with_guardian`, linked to guardian سارة (customer 2). `create_enrollment_with_subscription` correctly rejected the receptionist (no `enrollment.create`, real permission scoping caught), succeeded for the owner. Payment correctly rejected for the owner (no open cash shift — real cash-custody discipline caught), succeeded for the receptionist (has the open shift from Section 8). Attendance session + `mark_attendance` (coach, `session.manage`/`attendance.mark`). `get_player_360_summary` confirms correct financial/guardian/attendance/membership state. Identity isolation confirmed: exactly 1 `guardian_links` row in the whole club, correctly scoped to سارة↔يوسف only — zero leakage into Customer 1's (أحمد) data. |
| 17 | Cross-module customer consistency | PASS | | SERVER VERIFIED across Sections 9-16: `get_customer_360_summary` and direct invoice/payment/refund SQL agree exactly for Customer 1 (250 invoiced, 250 paid, 100 refunded, 2 bookings). `get_player_360_summary` agrees exactly with the enrollment/payment SQL for the academy family. No duplicate customer records (`upsert_customer`'s own dedup by phone). |
| 18 | Daily cash operations | PASS | | SERVER VERIFIED: `get_open_cash_shift_status` correctly computed expected cash (500 opening + 1150 collected − 100 refunded = 1550), matching the sum of all real cash transactions this session (booking 250, membership 500, academy 400 = 1150 gross; 100 refund). `close_cash_shift` with an exact matching count → `variance=0`, no liability. A second discrepancy test (expected 200, counted 150) correctly produced `variance=-50` and auto-created a real employee cash liability. Cash-custody enforcement genuinely caught in two places: a cash expense/payment attempt without an open shift for that specific staff member was correctly rejected both times (owner had permission but no shift). Non-cash (`bank_transfer`) expense recorded successfully via `record_expense` as the owner. |
| 19 | Report reconciliation | PASS | | SERVER VERIFIED: `get_revenue_report` shows `total_revenue=1150, refunds_total=100` — exact match to the raw booking (250) + membership (500) + academy (400) cash collected and the 100 refund. `get_expense_report` shows `total_expenses=50` — exact match to the bank_transfer expense recorded. No cross-module mismatch found. |
| 20 | Customer portal after operations | PASS | | SERVER VERIFIED (real-time RLS-scoped queries as the OTP-authenticated customer, no client cache involved so no staleness risk to test around): `get_my_portal_bookings` shows exactly both real bookings (confirmed + cancelled) with correct status/price. `get_my_portal_invoices` shows all 4 real invoices including the voided conflict-test one. `get_my_portal_club_memberships` shows both MEM-000001 (active) and MEM-000002 (pending_payment) correctly. All correctly scoped to the claimed customer identity. |
| 21 | Staff handoff | PASS | | ARCHITECTURALLY VERIFIED + SERVER VERIFIED: every RPC call throughout this journey used a fresh, independent session context (SQL-level JWT impersonation, no shared client-side cache) and each one independently resolved correct, current, live state (e.g. the coach's session correctly saw the booking/membership/enrollment state created by the receptionist and owner in prior "sessions" — visible in Section 16/18/19's cross-checks). Confirms the real architecture: authority is server-side (RLS + RPCs reading live DB state), not client-cached — so a genuine staff-B-logs-in-later scenario is safe by construction, not by coincidence. |
| 22 | Multi-branch (bounded) | PASS | | SERVER VERIFIED: second branch (`04d5a436`) + field (`bae6ff00`) created via `manage_branch`/`manage_field`. Branch-restricted receptionist correctly REJECTED ("not authorized for this branch") attempting a booking on the new branch's field. Owner (unrestricted, no `membership_branches` row) correctly SUCCEEDED on the same field — confirms the rejection is genuinely branch-scope enforcement, not a broader defect. |
| 23 | Tenant isolation regression | PASS | | SERVER VERIFIED: real Tenant B (`676ea358`) created with real customer/field/booking/invoice/player. From a Tenant A session, ALL 6 cross-tenant attacks correctly failed: (1) direct RLS-scoped read of Tenant B's customer → empty, no leak; (2) `get_customer_360_summary` with Tenant A's club_id + Tenant B's customer_id → "customer not found"; (3) same RPC with Tenant B's REAL club_id (attacker-known) + their customer_id → "not authorized"; (4) `create_booking` against Tenant B's real field → "not authorized"; (5) `record_payment` against Tenant B's real invoice → "invoice not found or you do not have permission"; (6) `create_enrollment_with_subscription` against Tenant B's real player → "player not found in this club". Every failure returned a clean, honest, non-crashing error — zero data leaked in any attempt. |
| 24 | Platform support journey | PASS | | SERVER VERIFIED: disposable QA platform-owner fixture used (never impersonated a real production platform owner). `get_platform_club_360` correctly identifies owner/counts for Tenant A without any manual SQL. `get_platform_club_staff_summary` correctly shows the real per-role staff breakdown. Both are real, existing routine-diagnosis tools — no manual SQL needed for the support scenario. |
| 25 | SaaS trial/active state | PASS | | SERVER VERIFIED implicitly by every successful write across Sections 6-24 (trial `club_write_allowed` correctly permitted normal operations) + explicitly by the suspend/reactivate test in Section 27. |
| 26 | SaaS renewal | PASS | | SERVER VERIFIED: `renew_platform_subscription` (Monthly plan, 499 EGP) correctly created a new `paid`/`active` subscription record starting exactly when the trial ends (2026-09-07), correct plan/price snapshot. Original trial record preserved unchanged as history — no in-place mutation. |
| 27 | Suspend/reactivate | PASS | | SERVER VERIFIED: `platform_suspend_club` correctly set `clubs.status='suspended'`; the already-authenticated receptionist's (stale-tab-equivalent) `create_booking` attempt correctly REJECTED server-side ("club subscription does not allow new bookings"). `platform_reactivate_club` correctly restored normal operation — the identical booking call succeeded immediately after. |
| 28 | Auth/role revocation | PASS | | SERVER VERIFIED: `deactivate_staff_member` on the coach, then the coach's still-authenticated session (old-tab equivalent) attempted `mark_attendance` → correctly rejected server-side ("not authorized for this session"). `reactivate_staff_member` correctly restored the fixture to its prior state. |
| 29 | Cache/context regression | PASS | | SERVER VERIFIED via Sections 27/28's own evidence: suspend→immediate-block, reactivate→immediate-restore, role-revoke→immediate-block, role-restore→immediate-restore — all transitions took effect on the VERY NEXT server call with no propagation delay or stale-permission window, since authority is resolved server-side per-request (RLS + RPC-internal `has_permission()` checks), never cached client-side. No stale tenant/customer data leakage possible by this architecture. |
| 30 | Printing | ENVIRONMENT-BLOCKED (partial) | | The underlying printable data is SERVER VERIFIED correct throughout this journey (invoice `a55a47f3` total/customer/booking all correct; correct receipt-relevant fields on every invoice created). The actual print/reprint UI interaction itself requires an authenticated staff browser session, which this session cannot drive (same standing never-type-a-password constraint documented in `E2E_TEST_STRATEGY.md`/`COMMERCIAL_RISK_REGISTER.md` for every prior phase). Not a defect — a disclosed, pre-existing tooling limitation. |
| 31 | Error recovery | PASS | | SERVER VERIFIED: double-submit (`record_payment` called twice with the identical idempotency key) returned the SAME payment id both times — confirmed exactly 1 payment row exists, 150 (not 300) allocated, no double-charge. Overpayment correctly rejected ("payment amount exceeds the invoice's outstanding balance") when I miscalculated my own test amount — a real, working guard, caught cleanly with no raw DB error. Invalid booking state (Section 11: complete-before-end, book-in-past), forbidden branch (Section 22), and blocked tenant (Section 27) all already independently verified with clear, non-raw errors and zero partial records in every case. |
| 32 | Concurrency (targeted) | PASS | | ARCHITECTURALLY CONCURRENCY VERIFIED + SERVER VERIFIED: `no_overlapping_field_bookings` is a genuine Postgres `EXCLUDE USING gist` constraint (atomically enforced by the engine, not a check-then-insert race) — same guarantee confirmed for `no_overlapping_subscription_periods` and `club_membership_subscriptions_no_overlap`. Payment idempotency is a genuine unique index `payments_club_idempotency_key_unique (club_id, idempotency_key)`, not app-level dedup — empirically confirmed in Section 31 (double-submit → 1 row, not 2). Single-request booking-conflict rejection already empirically proven live in Section 11. |
| 33 | Audit trail | PASS | | SERVER VERIFIED: full audit_logs query for Tenant A shows 50+ entries covering the ENTIRE real journey with correct who/what/tenant/entity/when on every row — platform actions (`platform_suspend_club`, `platform_reactivate_club`, `renew_platform_subscription`), staff actions (`staff.account_created_and_invited`, `staff.suspended`, `staff.reactivated`, and my own D-E2E-001 fix's `staff.membership_activated_on_login`), financial actions (`payment.record`, `payment.refund`, `invoice.issue`, `invoice.voided_on_booking_cancellation`, `employee_cash_liability.shortage_created`), and security-sensitive actions (`customer.self_service_claim`). No gaps found. |
| 34 | Notification linkage (bounded) | PASS | | SERVER VERIFIED: real journey actions correctly generated `notification_queue` entries — all `channel='email'` (zero `whatsapp` entries, WhatsApp genuinely untouched), correctly linked `club_id`/`recipient_customer_id`, correct `template_key` per real event (`booking-created`, `payment-received`, `booking-cancelled`), one correctly `cancelled` matching the booking-cancellation cascade. No delivery/retry/template auditing performed (out of scope per the directive). |
| 35 | RTL/LTR | PASS | | LIVE VISUALLY VERIFIED in production: customer OTP page and `/pricing` (real customer names, phones, money, dates all pass through this journey) correctly RTL-mirrored — nav/logo layout, text alignment, all correct. Pricing figures render with Arabic-Indic numerals and correctly match the real `platform_plans` values used in Section 26 (499/1349/2499/4499 EGP). |
| 36 | Responsive (375/768/1024/1440) | PASS | | LIVE VISUALLY VERIFIED: 375px (mobile, screenshotted, no overflow, correct RTL mirroring) and 768px (tablet, screenshotted, clean 4-card pricing grid). 1024px and 1440px confirmed via direct `scrollWidth`/`clientWidth` check: zero horizontal overflow at either. |
| 37 | Accessibility basics | PASS | | LIVE VERIFIED: real `<label for>` correctly associated (`portal-email` ↔ "البريد الإلكتروني"). Genuine keyboard Tab navigation correctly triggers `:focus-visible` with a real, visibly distinct box-shadow ring (confirmed the difference between focused/unfocused computed styles using actual Tab key presses — an earlier JS-`.focus()`-based check gave a false negative since synthetic focus doesn't reliably trigger `:focus-visible`, corrected by testing the real interaction). `role="alert"` on form errors already confirmed rendering correctly in production (Section 10's wrong-OTP screenshot). |
| 38 | Data cleanup | ACCEPTED LIMITATION | | The single trivially-disposable artifact (duplicate-retry club from Section 4) was cleaned immediately. The rest of this journey's data — Tenant A (`f8376f07`) and Tenant B (`676ea358`) and all their bookings/payments/refunds/invoices/audit logs/notification_queue entries — is left intentionally intact: it is now deeply interlinked real financial/audit history (payments referencing invoices referencing bookings, refunds referencing payments, audit_logs referencing all of the above), and per the directive's own instruction ("do not delete immutable financial/audit/historical records merely for cosmetic cleanup" + "if a QA record must remain because immutable history references it: document it") and Section 45's TRUE STOP protection on financial/operational data, a teardown risking orphaned references or broken constraints is not worth the cosmetic benefit. Both QA tenants are confirmed left in a clean `active` (not suspended) state — no leftover broken state. |

## Defect register

| ID | Summary | Severity | Status | Fix commit |
|---|---|---|---|---|
| D-E2E-001 | An invited staff member (created via `create_club_staff_membership_service`, the real staff-creation path) can never actually gain access: no code anywhere (client, RPC, trigger, edge function) ever transitions `club_memberships.status` from `invited` to `active`, despite the introducing migration's own comment claiming "flipped to active on first login." The invited staff member's real first sign-in hits `LoginPage.tsx`'s `hasAnyActiveMembership()`, which filters `.eq('status','active')` — an `invited` row doesn't count, so they're routed to `/onboarding` (create a brand-new club) instead of their real invited club. Reproduces the exact same class of bug as the already-fixed Final Pre-Release Verification defect ("user with zero club_memberships landing with no path forward"), just for the `invited` state specifically. | P0 | FIXED + PASS | new migration `20260831140000_activate_invited_staff_membership_on_login.sql` (governed RPC `activate_my_invited_memberships()`, self-scoped to `auth.uid()`, idempotent, audited) + `LoginPage.tsx` wired to call it post-sign-in before routing. SERVER VERIFIED live (real QA staff activated, idempotency + self-scoping both confirmed) + AUTHENTICATED AUTOMATED VERIFIED (5 new unit tests, `LoginPage.test.tsx`, including a call-order regression assertion). Committed in the final Phase batch (see regression gate). |

## Regression gate (final, pre-deploy)

TSC = PASS (clean) · LINT = PASS (0 errors, 19 pre-existing warnings) ·
UNIT = PASS (169/169, 5 new in LoginPage.test.tsx) · TARGETED INTEGRATION
= PASS (live SQL-level RPC verification across the full journey, this
document) · E2E = PASS (20/20, e2e/auth + e2e/public) · CI = PENDING ·
PRODUCTION = PENDING

## Notes

(running log of decisions, ambiguities, and evidence appended during
execution)
