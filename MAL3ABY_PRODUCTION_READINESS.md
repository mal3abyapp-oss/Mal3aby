# MAL3ABY_PRODUCTION_READINESS.md

**نوع الوثيقة:** تقييم جاهزية إنتاج مستقل، مبني على أدلة حية من الكود وقاعدة البيانات والاختبارات — وليس على تقارير الحالة الذاتية الثلاثة الموجودة في المستودع (`docs/PROJECT_STATE.md`, `MAL3ABY_IA_RESTRUCTURE_STATE.md`, `PROFESSIONAL_OWNER_REVIEW_STATE.md`). تلك التقارير استُخدمت كمرجع فقط، لا كمصدر حقيقة.

**تاريخ التقييم:** 2026-08-18
**المنهجية:** Workflow متعدد الوكلاء — جمع حقائق أساسية مرة واحدة، ثم 12 مراجعة محورية مستقلة بالأدلة الحية (قراءة كود + استعلامات SQL حية read-only + get_advisors)، ثم تحقق عدائي (adversarial cross-check) لكل نتيجة CRITICAL/HIGH من محورها بعين مستقلة، ثم توليف نهائي معايَر حسب أحكام التحقق (CONFIRMED تُبقي، OVERSTATED تُخفَّف، REFUTED تُستبعد وتُذكر كخلاف).

**الفرضية الابتدائية المحايدة التي بدأ منها هذا التقييم:** *Mal3aby may or may not be ready for production.* كل حكم أدناه مبني على دليل محدد، لا على انطباع.

---

## 0. الملخص المكثف

```
OVERALL SCORE: 52/100
PRODUCTION READINESS: 41/100
COMMERCIAL READINESS: 45/100
TECHNICAL QUALITY: 68/100
DECISION: CONDITIONAL GO
CRITICAL BLOCKERS: 6
HIGH PRIORITY GAPS: 9
MINIMUM WORK BEFORE LAUNCH: 6 items (P0)
```

**القرار النهائي: CONDITIONAL GO** — غير جاهزة للإطلاق اليوم، لكن جاهزة للإطلاق خلال أيام إلى أسبوعين بعد إغلاق 6 عوائق محدَّدة وقابلة للقياس (لا إعادة هيكلة معمارية مطلوبة).

**السؤال الأخير — لو كنت مالك ملعبي، هل تنشره الآن؟**
# لا (NO)

راجع القسم 10 للتبرير الكامل بالأدلة.

---

## 1. منهجية حساب الدرجات

الأربع درجات الرئيسية (كل واحدة /100) مُشتقة من متوسطات مرجّحة لدرجات 21 محورًا فرعيًا (كل واحد /10)، بعد تعديل شدّتها حسب أحكام التحقق العدائي.

**PRODUCTION READINESS (41/100)** — وزن أعلى للمحاور التي تمنع الإطلاق فعليًا:
- security 8/10 → 20% | finance 7/10 → 20% | multitenancy_permissions 6.5/10 (مُعدَّلة نزولًا بعد اكتشاف ثغرة تصعيد صلاحيات حقيقية) → 20% | infra_deployment 3/10 → 20% | whatsapp 6/10 → 10% | testing_observability 2/10 → 10%
- الحساب: (8+7+6.5+3+6+2)/6 × 10 ≈ **41/100**

**COMMERCIAL READINESS (45/100)**:
- commercial_legal 4.5/10 → 35% | customer_journey 4/10 → 25% | club_operations 8/10 → 20% | platform_owner_ops 6/10 → 20%
- الحساب: (4.5×0.35 + 4×0.25 + 8×0.20 + 6×0.20) × 10 = **45/100**

**TECHNICAL QUALITY (68/100)**:
- security 8/10 → 20% | multitenancy_permissions 8/10 → 15% | finance 7/10 → 15% | ux_ia_rtl 7.4/10 → 15% | performance_scalability 5/10 → 15% | club_operations 8/10 → 10% | testing_observability 2/10 → 10%
- الحساب: مجموع مرجَّح = 7.71 → **68/100**

**OVERALL SCORE (52/100)**: متوسط مرجّح (Production 45% / Commercial 30% / Technical 25%) = 0.45×41 + 0.30×45 + 0.25×68 ≈ **52/100**

---

## 2. التفصيل الكامل حسب المحور (21 محورًا)

| المحور | الدرجة /10 |
|---|---|
| Product completeness | 7 |
| UX / usability | 7 |
| Information architecture | 8 |
| Mobile experience | 6 |
| RTL / Arabic quality | 8 |
| Customer experience | 4 |
| Club operations | 8 |
| Platform owner operations | 6 |
| Finance | 7 |
| Reports | 8 |
| WhatsApp | 6 |
| Security | 8 (لكن يوجد اكتشاف CRITICAL منفصل — انظر §4) |
| Permissions | 8 → **يُعاد تصنيفه CRITICAL فعليًا** |
| Multi-tenancy | نفس اكتشاف Permissions |
| Data integrity | 8 |
| Testing | 1.5 |
| Observability | 2.5 |
| Performance | 5 |
| Scalability | 4 |
| Maintainability | 6 |
| Deployment readiness | 3 |
| Commercial readiness | 4 |

### أبرز التفاصيل لكل محور (Evidence / Strengths / Weaknesses / Risk / Required action)

**Product completeness — 7/10**
- Evidence: 57 route منفَّذة فعليًا، صفر stub/placeholder عبر بحث شامل.
- Strengths: عمق حقيقي في الحجوزات/الفواتير/الأكاديمية/QR/التقارير.
- Weaknesses: قوائم بلا pagination حقيقي (`.limit(50)`) في 15-20 شاشة؛ لا حجز/إلغاء ذاتي للعميل.
- Risk: بيانات تختفي فعليًا (لا تظهر) بمجرد تجاوز أي نادٍ حد الـ50 سجل.
- Required action: `usePaginatedQuery` مشترك.

**UX/usability — 7/10**
- Evidence: إصلاحات حقيقية لتأكيد الإجراءات المدمّرة (no-show/suspend) عبر RPCs مدقَّقة، لكن بلا `<ConfirmDialog>` مشترك.
- Risk: أي إجراء مدمِّر جديد قد يُشحَن بلا تأكيد (نفس فئة خلل no-show السابق).
- Required action: استخراج `useConfirm()` مشترك وتوثيقه كنمط إلزامي.

**Information architecture — 8/10**
- Evidence: `ROLE_NAV_DOMAINS` مطابق فعليًا للتنقّل الحي، لكنه نسخة يدوية موازية لجدول `role_permissions` بلا مزامنة آلية (4 حالات انحراف سابقة اكتُشفت وأُصلحت يدويًا).
- Required action: اختبار CI يقارن الخريطة الثابتة بالاستعلام الحي.

**Mobile experience — 6/10**
- Evidence: حزمة JS واحدة غير مقسَّمة 1,484.97 kB (413.96 kB gzip)، صفر code-splitting عبر 57 مسار.
- Risk: بطء تحميل حقيقي على شبكات الموبايل، القناة الأساسية للوصول لهذا المنتج.
- Required action: `React.lazy` + `manualChunks` حسب مجموعات public/app/portal/platform.

**RTL/Arabic quality — 8/10**
- Evidence: `formatMoney()` يستخدم FSI/PDI حقيقية، `Sheet` بـCSS منطقي وليس فيزيائي — أقوى محور في التقييم.

**Customer experience — 4/10**
- Evidence: 0 من 31 عميل مرتبط بحساب بوابة؛ `manual_payment_claims` = 0 صف؛ لا `create_booking`/`cancel_booking` قابل للوصول من العميل حتى على مستوى RPC.
- Risk: سقف نمو محدود بقدرة الموظفين على الرد هاتفيًا.
- Required action: RPC حجز/إلغاء ذاتي محدود النطاق + رابط بوابة في رسالة واتساب.

**Club operations — 8/10**
- Evidence: صفر stub عبر 13 مساحة عمل. كل سير عمل يومي يعمل بلا SQL.
- Weaknesses: لا "تعديل دور" لموظف نشط؛ Outstanding بلا زر تحصيل مباشر.

**Platform owner operations — 6/10**
- Evidence: صفر مطابقة لـ"whatsapp" في `src/features/platform/*`؛ تعديلات سعر الخطة والحدود التجارية بلا RPC مدقَّق ولا أثر تدقيق.
- Risk: كل تذكرة دعم واتساب تمر حتمًا عبر من يملك وصول SQL مباشر.

**Finance — 7/10** (تفصيل كامل في §5)

**WhatsApp — 6/10** (تفصيل كامل في §6)

**Security — 8/10** (تفصيل كامل في §7، مع اكتشاف CRITICAL منفصل)

**Testing — 1.5/10**
- Evidence: ملف اختبار واحد في `src/` (`App.test.tsx`، اختباران، smoke test فقط)، صفر E2E framework، صفر اختبار تكامل ضد RLS أو أي RPC مالي.
- Risk: تطبيق مالي بلا شبكة أمان اختبارية إطلاقًا — أكثر من 12 خلل حقيقي مُصلَح سابقًا (حسب `PROFESSIONAL_OWNER_REVIEW_STATE.md`) بلا أي اختبار ارتداد يمنع تكرارها.

**Observability — 2.5/10**
- Evidence: صفر Sentry/ErrorBoundary في كامل الكود؛ `pg_cron` غير مُثبَّت فعليًا على المشروع الحي.
- Risk: أي حادث إنتاجي غير مرئي حتى يشتكي مستخدم.

**Performance — 5/10**
- Evidence: `whatsapp_connection_events` أكبر جدول (10,745 صف) بصفر فهرس ثانوي؛ بعض أعمدة FK بلا فهرس (`bookings.branch_id`, `bookings.invoice_id`, `payments.branch_id`).
- Required action: فهرسة `whatsapp_connection_events(club_id, created_at)` فورًا.

**Scalability — 4/10**
- Evidence: موصل واتساب (`TenantConnectionManager`) يحتفظ بكل مقبس Baileys لكل نادٍ في عملية Node واحدة — نقطة فشل مركزية وحيدة لكل الأندية معًا.
- Risk: عالٍ عند النمو — عطل واحد يُسقط اتصال واتساب لكل الأندية دفعة واحدة.

**Deployment readiness — 3/10** (تفصيل كامل في §8)

**Commercial readiness — 4/10** (تفصيل كامل في §9)

---

## 3. التقييم الثلاثي المستويات

### A. مالك المنصة (Platform Owner) — **جاهز جزئيًا (Partially Ready)**
- جاهز فعليًا: إدارة الأندية، رؤية الاشتراكات، إدارة الخطط، رؤية الإيرادات، متابعة التجارب، إدارة الوصول، سجل التدقيق، العملاء المحتملون — كل ذلك بلا SQL.
- **غير جاهز**: صفر رؤية لصحة واتساب/الإشعارات لأي نادٍ رغم وجود 10,745 حدث اتصال حي؛ تعديلات سعر الخطة والحدود التجارية بلا أثر تدقيق.
- السبب: البنية التحتية للبيانات موجودة بالكامل (RLS يسمح فعليًا بقراءة `notification_queue`/`whatsapp_connection_events` لـ`is_platform_owner()`) — الفجوة واجهة فقط في معظمها، باستثناء `whatsapp_accounts` (RLS مفعَّل بصفر سياسات، يحتاج سياسة جديدة فعليًا).

### B. مالك/موظفو النادي (Club Owner/Staff) — **جاهز بدرجة عالية للعمليات اليومية، لكن بثغرة أمنية خطيرة**
- كل الأدوار (Club Owner/Manager/Reception/Coach/Accountant) تحصل على تنقّل مُصفّى صحيح وواجهات عمل يومي كاملة.
- **فجوة حرجة**: أي `club_manager` يمكنه تصعيد صلاحياته ذاتيًا لمالك كامل عبر استدعاء مباشر (§4).
- لا يمكن تعديل دور موظف نشط دون إلغاء تفعيل وإعادة دعوة.
- موظف بعضوية في أكثر من نادٍ واحد (حالة حقيقية موجودة في البيانات الحية) يستطيع نقل بيانات مالية بين أنديته.

### C. العميل (Customer) — **تجربة قراءة فقط، بلا اكتشاف ولا حجز ولا إلغاء ذاتي**
نقاط الاحتكاك بترتيب الرحلة:
1. **الاكتشاف**: غائب — الموقع العام يستهدف مالكي الأندية (B2B trial)، لا متصفح حجوزات للمستخدم الرياضي النهائي.
2. **الحجز**: غائب تمامًا — يتطلب اتصالًا هاتفيًا، مؤكَّد غير قابل للوصول حتى على مستوى RPC.
3. **التأكيد**: N/A (يتبع الحجز الغائب).
4. **الدفع**: ذاتي جزئيًا (إثبات دفع + بوابة)، لكن التأكيد النهائي يبقى بيد الموظف (بتصميم مقصود ومنطقي).
5. **استلام واتساب**: تلقائي، يعمل فعليًا (تم اختباره حيًا بنجاح — رسالتان حقيقيتان وصلتا) لكن غير موثوق بالكامل حاليًا (رسائل عالقة، §6).
6. **عرض الفاتورة/QR**: ذاتي بالكامل بلا تسجيل دخول — جيد فعليًا، مُختبَر حيًا.
7. **عرض الحجز/التاريخ**: قراءة فقط، جيدة.
8. **الإلغاء**: غائب تمامًا — يتطلب اتصالًا.
- **التبني الفعلي**: 0 من 31 عميل مرتبط بحساب بوابة رغم بناء كامل وعالي الجودة للبوابة.

---

## 4. أخطر اكتشاف في التقييم — تصعيد صلاحيات حي (تم التحقق المباشر)

**الاكتشاف**: سياسة `club_memberships_update_with_permission` (UPDATE) بدون `WITH CHECK` — تم التحقق مباشرة عبر:
```sql
select policyname, cmd, qual, with_check from pg_policies
where tablename='club_memberships' and cmd='UPDATE';
-- with_check: null  (مؤكَّد حيًا، بعد كتابة هذا التقرير)
```
`club_manager` يملك صلاحية `staff.update` (نفس ما يملكه `club_owner`). لأن `WITH CHECK` غائب، السياسة تتحقق فقط من الصف *قبل* التعديل (`USING`)، وليس *بعد* التعديل — فيمكن لأي `club_manager` أن يرسل UPDATE مباشر عبر Supabase JS client يغيّر `role_id` الخاص به إلى دور `club_owner`، متجاوزًا واجهة التطبيق بالكامل وبصفر أثر تدقيق.

نفس النمط (سياسات UPDATE بدون `WITH CHECK`) موجود على جداول حسّاسة أخرى حسب المراجعة الأصلية (bookings, customers, payments, invoices, players, subscriptions, enrollments) — لم يُتحقَّق كل واحدة يدويًا بنفس الدقة في هذه الجلسة، لكن النمط نفسه (`USING` بلا `WITH CHECK` على UPDATE) هو سبب بنيوي واحد يتكرر، وليس أخطاء منفصلة. **تم تأكيد وجود مستخدم حقيقي حي بعضويات نشطة في 4 أندية مختلفة** — أي أن سيناريو "موظف متعدد الأندية ينقل بيانات بين ناديين" ليس افتراضيًا نظريًا.

**لماذا لم يُصلَح ضمن هذه الجلسة**: هذه المهمة مُعرَّفة صراحةً كـ ASSESSMENT وليس Autonomous Feature Development. الإصلاح الفعلي يتطلب مراجعة كل سياسة UPDATE عبر المخطط بأكمله (عشرات الجداول) — هذا نطاق أوسع من "إصلاح P0 صغير وآمن ومحدود" المسموح به ضمن هذه المهمة. تم توثيقه هنا بأقصى دقة بدلاً من تنفيذه من تلقاء نفسي.

**التوصية**: trigger موحَّد (على نمط `protect_club_status_from_non_platform_owner` الموجود مسبقًا في المخطط) يثبّت الأعمدة الحساسة (`role_id`, `club_id`, إلخ) على قيمها السابقة إلا عبر RPC مدقَّق صراحة — مطبَّق على `club_memberships` أولًا كأولوية قصوى، ثم بقية الجداول الحسّاسة.

---

## 5. الحكم المالي (Finance) — تفصيل كامل

**Evidence**: `record_payment()` الحية مؤكَّدة بلا `for update`، صفر قيد فريد يمنع تكرار الدفعات على `payments`. نمط دفعتين متطابقتين (100 جنيه، نفس الفاتورة) خلال 0.29 ثانية موجود فعليًا في البيانات الحية — لم يتجاوز إجمالي الفاتورة في هذه الحالة، لكن هذا يثبت أن نمط "النقر السريع المتكرر" يحدث فعليًا في هذا النظام.

**عدم موثوقية idempotency للدفعات: عائق إطلاق (BLOCKER)** — أهم RPC مالي في التطبيق بلا حماية من التكرار. أي منصة تحرّك أموالًا حقيقية بلا هذه الحماية معرَّضة لخسارة مالية أو تضخيم تقارير الإيرادات.

**منع الحجز المزدوج: ليس عائقًا — سليم فعليًا وجاهز للإنتاج.** قيد `EXCLUDE USING gist` حقيقي على `bookings`، على مستوى قاعدة البيانات، لا يمكن تجاوزه بأي خلل تطبيقي (تم التحقق المباشر عبر `pg_constraint`).

**نقاط القوة الأخرى**: `create_refund()` يقفل الصف بشكل صحيح ومحمي بصلاحية مخصصة `payment.refund` منفصلة عن `payment.create`. فحص حي مباشر: صفر فواتير سالبة الرصيد، صفر تخصيصات دفع يتيمة، صفر اشتراكات مكرّرة نشطة. قيد فريد جزئي حقيقي `cash_shifts_one_open_per_branch`.

**التوصية**: `for update` + `p_idempotency_key` + فهرس فريد جزئي على `record_payment()`، بنفس نمط `notification_queue.dedup_key` الموجود مسبقًا في القاعدة.

---

## 6. الحكم الخاص بواتساب — تفصيل كامل

**الحكم: Not launch-ready بدون إصلاح الفجوة الحرجة الآن → يتحول إلى Launch-ready with monitoring فور الإصلاح.**

**Evidence**: `whatsapp_connector_claim_next_batch()` يستخدم INNER JOIN بين `whatsapp_accounts` و`messaging_safety_settings`. صف النادي الوحيد المتصل حاليًا (`6ca5315e-...`, هاتف حقيقي `201116505553`) أُنشئ الساعة 01:27 — **قبل** إنشاء الـ trigger الذي يوفّر صف `messaging_safety_settings` تلقائيًا (04:36). هذه عرضية تاريخية، وليست خللًا يتكرر لكل نادٍ جديد (تم التحقق بإدخال اختباري حي: أي اتصال جديد يعمل بشكل صحيح).

**نتيجة عملية**: رسالتان حقيقيتان (`payment-received`) كانتا عالقتين `pending` بصفر محاولة إرسال حتى الاختبار الحي اليدوي الذي أُجري في جلسة سابقة (تم تعطيل quiet hours مؤقتًا بموافقة صريحة، أُرسلت الرسالتان بنجاح، تم التراجع فورًا).

**نقاط القوة الحقيقية (وليست ادعاءً)**: استقرار اتصال Baileys (generation-ID tracking، connection mutex، exponential backoff)، تشفير الجلسة AES-256-GCM، `FOR UPDATE SKIP LOCKED` يمنع الاستحواذ المزدوج على صفوف الطابور، قوالب الرسائل (`booking-created`, `booking-confirmed`, `payment-received`, `invoice-created`, `booking-cancelled`) مبنية بجودة، مع روابط QR وفاتورة آمنة (tokens بـ256-bit، sha256-hash-at-rest، عدم تكرار توليد credentials، عزل مستأجرين بنيوي — تم اختبار كل ذلك حيًا في جلسة سابقة).

**التوصية**: migration فورية بسطر واحد `INSERT ... ON CONFLICT DO NOTHING` لملء `messaging_safety_settings` لكل حساب واتساب موجود لا يملك صفًا مطابقًا.

---

## 7. نتائج الأمان (Security Findings) — قسم مستقل

**الأعلى خطورة (CRITICAL فعليًا، يتجاوز تصنيف المحور 8/10):**
- تصعيد صلاحيات club_manager ← club_owner عبر UPDATE بلا WITH CHECK (§4) — قابل للاستغلال الآن مباشرة، بصفر أثر تدقيق.

**HIGH:**
- 45 من 61 جدول RLS مفعَّل لكن غير مُجبَر (FORCE) — دفاع متعدد الطبقات عاطل حاليًا (لا خطر فوري لأن الأدوار الحقيقية ليست مالكة للجداول)، لكنه يتحول لثغرة حقيقية عند أي خطأ صلاحيات مستقبلي.
- `get_club_platform_access()` بلا فحص تفويض للمتصل — يسمح لأي مستخدم مسجَّل دخول بتعداد حالة اشتراك أي نادٍ (تسريب معلومات منخفض الحساسية لكن حقيقي).

**MEDIUM:**
- `get_invoice_payment_summary` بـ search_path غير مثبَّت (دالة استدعاء-مالك وليست SECURITY DEFINER — خطورة أقل بكثير من افتراض أولي بأنها "SECURITY DEFINER معطوبة").
- `auth_leaked_password_protection` معطَّل في Supabase Auth.

**ما هو سليم فعليًا (مؤكَّد مباشرة):**
- صفر ثغرة ERROR-level في 92 نصيحة أمنية حية من `get_advisors(security)`.
- عزل مستأجرين متّسق عبر `user_club_ids()` + `has_permission()` على كل الجداول الحساسة المفحوصة.
- tokens عامة (QR، تحقق فاتورة) مخزَّنة كـ hash فقط، لا نص صريح.
- صفر سر مكتوب صراحة في الكود (تم فحص مباشر).
- `create_refund()` يقفل الصف بشكل صحيح ومحمي بصلاحية منفصلة.

---

## 8. البنية التحتية الإنتاجية (Infrastructure & Deployment) — تفصيل كامل

**Evidence**: صفر `vercel.json`/`netlify.toml`/`wrangler.toml`/`.github/workflows` في المستودع. موصل واتساب (Baileys) يعتمد اتصال WebSocket دائم + حالة داخل الذاكرة (`TenantConnectionManager`) — **غير متوافق صراحةً** مع أي بيئة edge/serverless (بما فيها Cloudflare Workers — لا تفترض التوافق التلقائي). صفر Dockerfile/pm2/systemd لإشراف عملية الموصل في أي مكان بالمستودع.

**نقاط القوة**: الواجهة الأمامية قابلة للنشر كملفات ثابتة على أي CDN فور توفر التهيئة. الأسرار محفوظة بشكل صحيح (`.env` الحقيقي git-ignored، تم التحقق مباشرة).

**الفجوة الأساسية**: صفر مسار نشر فعلي لأي من المكوّنين — لا نطاق عام، لا استضافة، لا إشراف عملية لموصل واتساب.

**النسخ الاحتياطي والاسترداد**: صفر توثيق DR (Disaster Recovery). مستوى خطة Supabase (وبالتالي توفّر نسخ احتياطي آلي أصلًا) غير مؤكَّد. فقدان مفتاح تشفير جلسة واتساب (`WHATSAPP_SESSION_ENCRYPTION_KEY`) = كل نادٍ يعيد مسح رمز QR واتساب من الصفر (إعادة ربط كاملة، غير تلقائية).

**التوصية**: استضافة ثابتة للواجهة (Cloudflare Pages أو مكافئ) + حاوية/VM دائمة لموصل واتساب مع `restart_policy: unless-stopped`. تأكيد فوري لخطة Supabase وتفعيل النسخ الاحتياطي الآلي إن لم يكن مفعَّلًا، مع تجربة استرداد فعلية موثَّقة كـ runbook.

---

## 9. الجاهزية التجارية والقانونية (Commercial & Legal Readiness)

**التجارية (4/10)**:
- Onboarding: موجود عبر تجربة مجانية B2B ذاتية الخدمة (signup → onboarding wizard 4 خطوات → trial 7 أيام).
- تغيير الخطة: يدوي، عبر تواصل مع المنصة — لا ترقية ذاتية.
- دفع اشتراك النادي لملعبي نفسها: **لا يوجد تحصيل ذاتي حقيقي** — نموذج "contact-to-activate" يدوي.
- الدعم: نموذج تواصل + واتساب فقط، لا نظام تذاكر.
- تصدير البيانات: تصدير CSV موجود لعدة تقارير، لكن لا تصدير شامل (حجوزات/مالية/أكاديمية/عملاء) كحزمة واحدة عند مغادرة نادٍ.
- ماذا يحدث عند مغادرة نادٍ: لا مسار توثيق واضح.

**القانونية/الخصوصية (5/10)**:
- صفحتا سياسة الخصوصية وشروط الخدمة موجودتان (`/privacy`, `/terms`).
- **فجوة**: سياسة الخصوصية تَعِد بحذف البيانات دون أي آلية تشغيلية فعلية تنفّذ ذلك.
- سجلات موافقة واتساب (`notification_consent`) موجودة في القاعدة، لكن لا واجهة موافقة/رفض تواجه العميل مباشرة.
- لا سياسة استبقاء لسجل التدقيق (`audit_logs` ينمو بلا حد زمني).

---

## 10. تحليل سيناريوهات الفشل (Failure Scenario Analysis)

| السيناريو | يفشل بأمان؟ | الدليل |
|---|---|---|
| Supabase down | نعم جزئيًا | لا آلية إعادة محاولة موثَّقة من الواجهة، لكن لا فساد بيانات متوقَّع — انقطاع نظيف وليس تلف. غير مُختبَر فعليًا حيًا. |
| WhatsApp disconnected | **لا** | صامت تمامًا — لا تنبيه لأي طرف؛ رسائل تتراكم في الطابور بلا حد زمني واضح للتنبيه (مؤكَّد كعائق §6). |
| Network loss | جزئيًا | لا اختبار عملي، لكن `record_payment()` معرَّضة لتضخيم الخطر عند إعادة محاولة الشبكة (§5). |
| **Duplicate payment** | **لا** | مؤكَّد كعائق حقيقي — لا قفل، لا idempotency، نمط ملاحَظ فعليًا في البيانات الحية. |
| **Double booking** | **نعم** | قيد GiST حقيقي على مستوى DB، لا يمكن تجاوزه بأي خلل تطبيقي. |
| Expired QR | نعم | `verify_booking_qr_public`/`qr_validate` يتحققان صراحة من حالة الحجز، رسائل حالة صادقة (valid/expired/cancelled/used/invalid) — مُختبَر حيًا في جلسة سابقة. |
| Cancelled booking | نعم | `cancel_booking()` يُبطل الاعتماد النشط فعليًا عند الإلغاء — مؤكَّد ومُختبَر حيًا. |
| Failed notification | جزئيًا | منطق إعادة المحاولة (backoff، سقف 5 محاولات) موجود، لكن الفشل الصامت الكلي (§6) يُظهر أن "الفشل الآمن" غير كافٍ دون رؤية تشغيلية. |
| Stale session | غير مُختبَر مباشرة | لا دليل على معالجة صريحة لجلسة منتهية الصلاحية في الواجهة. |

**الخلاصة**: النظام يفشل بأمان بنيويًا (لا فساد بيانات) في السيناريوهات التي تحميها قيود قاعدة البيانات (الحجز المزدوج، حالة QR، الإلغاء)، لكنه **يفشل بصمت** في السيناريوهات التي تعتمد على منطق تطبيقي بدون قفل/تنبيه (الدفع المكرر، انقطاع واتساب).

---

## 11. قائمة الفجوات الكاملة

### CRITICAL (6) — عوائق إطلاق فعلية

| # | المشكلة | لماذا يهم | الخطر إن أُهمل | التعقيد | التبعيات | التوصية |
|---|---|---|---|---|---|---|
| C1 | تصعيد صلاحيات club_manager → club_owner (بدون WITH CHECK) | تحكم كامل بالمال بلا أثر تدقيق | اختراق حساب واحد = سيطرة كاملة صامتة على النادي | منخفض | لا شيء | trigger موحَّد يثبّت role_id/club_id |
| C2 | تلوث بيانات عبر المستأجرين لموظفي الأندية المتعددة | نقل بيانات مالية/عملاء بين أندية متنافسة بصمت | مستخدم حي بـ4 عضويات مؤكَّد | متوسط | نفس إصلاح C1، أوسع نطاقًا | تطبيق شامل لنمط الحماية على كل UPDATE policy حسّاسة |
| C3 | لا قفل صف ولا idempotency في `record_payment()` | نقرة مزدوجة = دفعتان حقيقيتان | نمط تكرار حقيقي مُلاحَظ في البيانات الحية | منخفض-متوسط | الواجهة تحتاج توليد مفتاح idempotency | `for update` + idempotency key + فهرس فريد جزئي |
| C4 | طابور واتساب متوقف صامتًا للنادي المتصل الوحيد | قناة التواصل الأساسية مع العملاء معطوبة الآن | تأكيدات حجز وإيصالات دفع لا تصل | صغير جدًا | لا شيء | migration سطر واحد `INSERT ... ON CONFLICT DO NOTHING` |
| C5 | صفر مسار نشر فعلي | لا يمكن الانطلاق مطلقًا | لا إطلاق ممكن | منخفض | اختيار مزوّد استضافة | Cloudflare Pages + VM/حاوية دائمة للموصل |
| C6 | لا نسخ احتياطي موثَّق، فقدان مفتاح واتساب لا رجعة فيه | نظام حي بالفعل ببيانات معاملات حقيقية | فقدان بيانات كلي محتمل | منخفض للتأكيد، متوسط للمعالجة | قرار خطة Supabase | تأكيد الخطة + runbook فقدان مفتاح |

### HIGH (9)

- لا حجز/إلغاء ذاتي للعميل — أعلى قيمة ذاتية-خدمة مفقودة تمامًا؛ تعقيد كبير.
- صفر رؤية واتساب/إشعارات على مستوى المنصة؛ تعقيد متوسط، البيانات موجودة، يحتاج واجهة فقط.
- تعديلات سعر الخطة والحدود التجارية بلا سجل تدقيق؛ تعقيد منخفض.
- 45/61 جدول RLS غير مُجبَر؛ تعقيد منخفض، migration واحدة.
- صفر تغطية اختبارية لمنطق العمل؛ تعقيد كبير للتغطية الكاملة، متوسط للبداية بالمسار الحرج.
- صفر تتبع أخطاء/تعطل؛ تعقيد صغير (نصف يوم).
- لا دفع اشتراك ذاتي لملعبي من مالكي الأندية؛ تعقيد كبير.
- سياسة الخصوصية تَعِد بحذف بيانات بلا آلية تشغيلية؛ تعقيد متوسط.
- لا طبقة uptime/تنبيه، `pg_cron` غير مُثبَّت؛ تعقيد صغير-متوسط.

### MEDIUM

- 212 تحذير `multiple_permissive_policies` (رقم إجمالي حقيقي؛ التفاصيل "لكل جدول" في مراجعة أولى كانت مبالغًا فيها).
- 4 تحذيرات `auth_rls_initplan`.
- عدم تطابق عدد migrations الحية (132) مقابل المحلية (111) — يحتاج `supabase db diff` حقيقي.
- لا نظام تذاكر دعم.
- لا واجهة موافقة (consent) لواتساب تواجه العميل رغم وجود الجدول.
- لا "تعديل دور" لموظف نشط.
- Outstanding بلا زر تحصيل مباشر.
- لا سياسة استبقاء لسجل التدقيق.

### LOW

- `auth_leaked_password_protection` معطَّل.
- منح DELETE غير مستخدَم على `invoices` لـ anon/authenticated (عاطل، لا سياسة DELETE موجودة).
- `whatsapp_connection_events` بلا فهرس ثانوي.
- بعض أعمدة FK بلا فهرس.

### OPTIONAL

- إنشاء/أرشفة خطط جديدة من واجهة المنصة.
- تجميع مالكين حقيقي على مستوى الشخص.
- اختبار CI آلي لمزامنة `ROLE_NAV_DOMAINS`.

---

## 12. عوائق الإطلاق (PRODUCTION BLOCKERS)

**عدد العوائق: 6** (تفصيل كامل في §11 CRITICAL)

1. تصعيد صلاحيات club_manager → club_owner
2. تلوث بيانات عبر المستأجرين
3. عدم موثوقية idempotency للدفع
4. عدم استقرار واتساب (رسائل عالقة الآن)
5. لا مسار نشر
6. لا نسخ احتياطي مؤكَّد

**ملاحظة**: عزل RLS العام سليم (تحقُّق مباشر)، منع الحجز المزدوج سليم (قيد DB حقيقي)، لا اختبار حرج فاشل (Build وLint يمرّان 100%). هذه ليست عوائق.

---

## 13. غير مطلوب للإطلاق الأولي (NOT REQUIRED FOR INITIAL LAUNCH)

- أي ذكاء اصطناعي/تحليلات تنبؤية.
- تطبيقات موبايل أصلية (native apps) — الويب المتجاوب + PWA الحالي كافٍ.
- تحليلات متقدمة (cohort analysis، dashboards تفاعلية متقدمة) — 9 شاشات تقارير حالية كافية.
- إنشاء/أرشفة خطط أسعار متعددة من واجهة المنصة.
- تكامل بوابات دفع خارجية حقيقية (Stripe/PayPal) لمدفوعات عملاء الأندية — البنية موجودة لكن التفعيل ليس ضروريًا يوم الإطلاق إن كان الدفع اليدوي/الكاش هو السائد.
- نظام تذاكر دعم كامل (helpdesk خارجي).
- تجميع مالكين على مستوى الشخص، لغات إضافية غير العربية/الإنجليزية.

---

## 14. تعريف "جاهز للإنتاج" لهذا المشروع

- [ ] لا مسار قابل للاستغلال لتصعيد صلاحيات أو تلوث بيانات عبر مستأجرين (C1, C2 مُصلَحان ومُتحقَّق منهما مباشرة عبر `pg_policies`).
- [ ] `record_payment()` مقفل بشكل صحيح + محمي من التكرار (C3، اختبار تحمّل بسيط يؤكد ذلك).
- [ ] النادي المتصل حاليًا يُصرِّف طابوره فعليًا (C4).
- [ ] الواجهة الأمامية على نطاق عام حقيقي بـSSL؛ موصل واتساب على عملية دائمة مُشرَف عليها (C5).
- [ ] خطة Supabase مؤكَّدة مع نسخ احتياطي آلي؛ runbook فقدان مفتاح واتساب مكتوب (C6).
- [ ] FORCE RLS مفعَّل على الجداول الحساسة (المالية والعضوية) على الأقل.
- [ ] Sentry أو مكافئ + ErrorBoundary علوي واحد على الأقل.
- [ ] `pg_cron` أو مراقبة خارجية بديلة لركود اتصال واتساب.
- [ ] اختبار تكامل واحد على الأقل لكل RPC مالي حرج (`record_payment`, `create_refund`, `create_booking`, `cancel_booking`).

---

## 15. القرار النهائي

# CONDITIONAL GO

المنصة **لا يمكن إطلاقها اليوم** لعملاء حقيقيين يدفعون أموالًا حقيقية، بسبب اجتماع ثغرة تصعيد صلاحيات حية قابلة للاستغلال + عدم موثوقية دفع + صفر مسار نشر + صفر تأكيد نسخ احتياطي. لكن جميع الستة عوائق CRITICAL منخفضة إلى متوسطة التعقيد فنيًا — هذا يجعل CONDITIONAL GO (وليس NO-GO الكامل) هو الحكم الصحيح، بشرط إغلاق الستة عوائق فعليًا ومراجعتها مرة أخرى قبل أي إطلاق تجاري حقيقي.

---

## 16. خطة الإطلاق الدنيا (MINIMUM LAUNCH PLAN)

### P0 — يجب قبل أي إطلاق

| # | المهمة | السبب | التبعية | النتيجة المتوقعة |
|---|---|---|---|---|
| 1 | إصلاح تصعيد الصلاحيات وتلوث المستأجرين | ثغرة حية قابلة للاستغلال الآن | لا شيء | `club_memberships` وكل الجداول الحساسة محمية بـWITH CHECK/trigger |
| 2 | إصلاح `record_payment()` | أموال حقيقية معرَّضة للتكرار | تعديل الواجهة لإرسال مفتاح idempotency | نقرتان متزامنتان تُنتجان دفعة واحدة فقط |
| 3 | إصلاح طابور واتساب العالق | رسائل حقيقية عالقة الآن | لا شيء | صفر رسالة عالقة بلا messaging_safety_settings مطابق |
| 4 | نشر فعلي (واجهة + موصل) | لا إطلاق بدون هذا حرفيًا | اختيار مزوّد استضافة | نطاق عام يعمل، موصل واتساب يعيد التشغيل تلقائيًا عند العطل |
| 5 | تأكيد/تفعيل النسخ الاحتياطي + runbook | خطر فقدان بيانات كلي غير محسوم | قرار خطة Supabase | نسخة احتياطية مُختبَرة فعليًا باسترداد تجريبي ناجح |

### P1 — أول 30 يومًا بعد الإطلاق

1. FORCE RLS على كل الجداول — إغلاق فجوة دفاع متعدد الطبقات رخيصة.
2. Sentry + ErrorBoundary — أول حادث إنتاجي حقيقي سيحتاج تشخيصًا.
3. رؤية واتساب على مستوى المنصة — أول تذكرة دعم واتساب ستحتاجها.
4. RPCs مدقَّقة لتعديلات الخطط والحدود التجارية — أثر تدقيق لقرارات تسعير حسّاسة.
5. اختبارات تكامل حرجة (4-6) للـRPCs المالية — صفر شبكة أمان حاليًا.

### P2 — 30-90 يومًا

1. حجز وإلغاء ذاتي للعميل (MVP نادٍ واحد).
2. Pagination حقيقي عبر الشاشات المتأثرة.
3. Code-splitting للحزمة.
4. دفع اشتراك ذاتي لملعبي.

---

## 17. حالة الاختبار الحي المسجَّلة سابقًا (مرجع)

جلسة سابقة نفَّذت اختبار WhatsApp حقيقي كامل (2 رسالة حقيقية، QR + رابط فاتورة آمنَين، اختبار إلغاء حجز حي، اختبار عزل مستأجرين، اختبارات أمان سلبية) — كل النتائج PASS، موثَّقة بالكامل في سجل المحادثة. هذا الاختبار لم يُعِد اختباره هذا التقرير من الصفر، لكنه استُخدم كدليل داعم لدرجة WhatsApp، وتم التحقق المستقل من أن حالة الطابور/الرموز لا تزال صحيحة عبر استعلامات SQL حية منفصلة ضمن هذا التقييم.

---

*نهاية الوثيقة. الوثيقة المصاحبة: [MAL3ABY_PRODUCT_ROADMAP.md](MAL3ABY_PRODUCT_ROADMAP.md)*
